# Kubernetes

This folder is reserved for deployment manifests once a local service is ready to promote. Recommended first targets:

- FastAPI gateway deployment and service
- Web deployment and service
- Qdrant stateful set or managed vector database connection
- GPU inference deployment backed by vLLM or SGLang
- Prometheus service monitor and Grafana dashboard config maps
